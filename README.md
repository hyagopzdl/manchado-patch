# Manchado Patch — Supabase direto

Projeto completo pronto para instalação em um projeto Supabase saudável.

Leia primeiro: `COMECE-AQUI.md`.

Esta versão remove a persistência por snapshots compatíveis com Firebase e usa as tabelas normalizadas do PostgreSQL, com gravações pela RPC `apply_direct_patch`.
