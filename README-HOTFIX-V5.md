# Hotfix V5

1. Execute `supabase/HOTFIX-PRESENCE-AND-FAVORITES.sql` in the Supabase SQL Editor.
2. Replace `js/supabase.js` in the deployed site.
3. Publish and reopen the app in a fresh private window.

This version keeps compatibility presence local-only and rewrites favorite persistence with unambiguous SQL variables.
